---
---

Record the initial one-fetch 0.1 Preview baseline: shared protocol and policy
core, Cloudflare/Supabase/Node adapters, the static administration client,
auditing controls, deployment tooling, and review-only release artifacts.

This Changeset is intentionally empty because every workspace package is
already bootstrapped at the coordinated 0.1.0 Preview version. Future changes
must select the affected private packages and an explicit bump level.
